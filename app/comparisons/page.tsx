import type{Metadata}from"next";import{SeoCollection}from"@/components/seo-collection";export const metadata:Metadata={title:"TokenWatch Comparisons",description:"Compare provider billing dashboards, application logging, and proactive AI budget controls."};export default function Page(){return <SeoCollection eyebrow="Comparisons" title="Beyond provider billing dashboards" description="TokenWatch adds cross-provider attribution and pre-request enforcement while provider consoles remain the source for provider invoices." items={[{title:"Provider dashboards",description:"Provider consoles show their own totals. TokenWatch normalizes usage across supported providers."},{title:"Application logs",description:"Logs explain failures. TokenWatch adds budgets, entitlements, alerts, and cost-aware policy decisions."},{title:"Spreadsheets",description:"Spreadsheets report after the fact. TokenWatch evaluates policy before the expensive request."}]}/>}
