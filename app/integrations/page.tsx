import type{Metadata}from"next";import{SeoCollection}from"@/components/seo-collection";export const metadata:Metadata={title:"AI Provider Integrations",description:"TokenWatch integrations for leading LLM providers and cloud AI platforms."};export default function Page(){return <SeoCollection eyebrow="Integrations" title="One control layer for your model providers" description="Use the official SDK or REST API from server-side applications." items={["OpenAI","Anthropic","Gemini","Groq","OpenRouter","Azure OpenAI","AWS Bedrock"].map(title=>({title,description:`Ingest ${title} token usage and evaluate organization policy with server-calculated cost.`}))}/>}
