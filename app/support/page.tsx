"use client";import{FormEvent,useState}from"react";import Link from"next/link";import{toast}from"sonner";import{createSupportTicket}from"@/lib/api";
export default function Support(){const[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);try{await createSupportTicket({category:String(f.get("category")),subject:String(f.get("subject")),message:String(f.get("message")),page_url:location.href});toast.success("Support request received");e.currentTarget.reset()}catch{toast.error("Sign in to send a support request")}finally{setBusy(false)}}return <main className="min-h-screen bg-slate-50 px-6 py-14"><div className="mx-auto max-w-3xl"><Link href="/help" className="text-blue-600">← Help center</Link><h1 className="mt-8 text-4xl font-semibold">Contact support</h1><p className="mt-3 text-slate-600">Send product, billing, bug, feedback, or security reports. Never include passwords or API keys.</p><form onSubmit={submit} className="mt-8 space-y-5 rounded-xl border bg-white p-6"><label className="block">Category<select name="category" className="mt-2 w-full rounded-lg border p-3"><option>support</option><option>billing</option><option>bug</option><option>feedback</option><option>security</option></select></label><label className="block">Subject<input required minLength={3} maxLength={160} name="subject" className="mt-2 w-full rounded-lg border p-3"/></label><label className="block">Message<textarea required minLength={10} maxLength={10000} rows={8} name="message" className="mt-2 w-full rounded-lg border p-3"/></label><button disabled={busy} className="rounded-lg bg-slate-900 px-5 py-3 text-white disabled:opacity-50">{busy?"Sending…":"Send request"}</button></form></div></main>}
