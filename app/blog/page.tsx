import type{Metadata}from"next";import Link from"next/link";export const metadata:Metadata={title:"Blog",description:"Engineering guidance for AI cost control and production LLM operations."};const posts=[{title:"Why AI budgets need a pre-request circuit breaker",date:"2026-08-01",summary:"Monitoring after a provider call cannot stop the spend that already happened."},{title:"Designing idempotent LLM usage ingestion",date:"2026-08-01",summary:"Replay windows and request identifiers make retries safe."}];export default function Page(){return <main className="min-h-screen bg-slate-50 px-6 py-14"><div className="mx-auto max-w-4xl"><Link href="/" className="text-blue-600">← TokenWatch</Link><h1 className="mt-8 text-4xl font-semibold">TokenWatch blog</h1><div className="mt-8 space-y-4">{posts.map(p=><article className="rounded-xl border bg-white p-6" key={p.title}><p className="text-xs text-slate-500">{p.date}</p><h2 className="mt-2 text-2xl font-semibold">{p.title}</h2><p className="mt-3 text-slate-600">{p.summary}</p></article>)}</div></div></main>}
