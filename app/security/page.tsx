import{LegalPage,Section}from"@/components/legal-page";export default function Page(){return <LegalPage title="Security" updated="August 1, 2026"><Section title="Tenant and access security"><p>Every organization-scoped request is authorized server-side. Database row-level security provides defense in depth, and privileged service credentials are never exposed to browsers.</p></Section><Section title="Credentials and payments"><p>Provider credentials are encrypted. SDK keys are stored as hashes. Payment data is collected and processed by Stripe; TokenWatch does not store card details.</p></Section><Section title="Operational security"><p>TokenWatch uses signed webhooks, idempotent billing processing, session revocation, structured audit logs, TLS, dependency pinning, and production error monitoring.</p></Section><Section title="Reporting"><p>Please report suspected vulnerabilities privately through the published support contact. Do not access data that is not yours.</p></Section></LegalPage>}
