"use client";import{FormEvent,useState}from"react";import Link from"next/link";import{register,resendVerification}from"@/lib/api";
export default function Page(){const[email,setEmail]=useState("");const[pending,setPending]=useState(false);const[loading,setLoading]=useState(false);const[error,setError]=useState("");const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setError("");const f=new FormData(e.currentTarget),p=String(f.get("password"));if(p!==f.get("confirm")){setError("Passwords do not match");return}setLoading(true);try{await register(email,p,String(f.get("name")));setPending(true)}catch{setError("Unable to create the account. Check the fields or sign in if you already registered.")}finally{setLoading(false)}};if(pending)return <main className="grid min-h-screen place-items-center bg-slate-50 p-4"><section className="w-full max-w-md rounded-2xl border bg-white p-8 text-center"><h1 className="text-2xl font-semibold">Verify your email</h1><p className="mt-3 text-slate-600">If the address can receive messages, verification instructions will arrive shortly. Check your spam folder before requesting another message.</p><button onClick={()=>resendVerification(email)} className="mt-5 rounded border px-4 py-2">Resend verification</button><Link href="/login" className="mt-4 block text-sm text-cyan-700">Return to sign in</Link></section></main>;return <main className="grid min-h-screen place-items-center bg-slate-50 p-4"><section className="w-full max-w-md rounded-2xl border bg-white p-8"><Link href="/" className="font-bold">TokenWatch</Link><h1 className="mt-6 text-3xl font-semibold">Create your account</h1><p className="mt-2 text-sm text-slate-500">Start with a secure organization and 10,000 monthly events.</p><form onSubmit={submit} className="mt-6 space-y-4"><label className="block text-sm">Full name<input required name="name" className="mt-1 w-full rounded border p-3"/></label><label className="block text-sm">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-1 w-full rounded border p-3"/></label><label className="block text-sm">Password<input required minLength={12} name="password" type="password" autoComplete="new-password" className="mt-1 w-full rounded border p-3"/></label><label className="block text-sm">Confirm password<input required minLength={12} name="confirm" type="password" autoComplete="new-password" className="mt-1 w-full rounded border p-3"/></label>{error&&<p role="alert" className="text-sm text-rose-700">{error}</p>}<button disabled={loading} className="w-full rounded bg-slate-900 p-3 font-medium text-white">{loading?"Creating account…":"Create account"}</button></form><p className="mt-5 text-center text-sm">Already registered? <Link href="/login" className="font-medium text-cyan-700">Sign in</Link></p></section></main>}
