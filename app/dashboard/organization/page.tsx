"use client";import{useWorkspace}from"@/components/app-shell";import{Card,PageHeader}from"@/components/page-header";export default function Page(){const{organization,role}=useWorkspace();return <div className="space-y-6"><PageHeader title="Organization settings" description="Workspace identity and access summary. Server-side membership checks protect every organization resource."/><Card><dl className="grid gap-5 sm:grid-cols-2"><div><dt className="text-sm text-slate-500">Name</dt><dd className="font-medium">{organization?.name||"Loading…"}</dd></div><div><dt className="text-sm text-slate-500">Your role</dt><dd className="capitalize font-medium">{role}</dd></div><div><dt className="text-sm text-slate-500">Status</dt><dd className="capitalize font-medium">{organization?.status}</dd></div><div><dt className="text-sm text-slate-500">Organization ID</dt><dd className="break-all font-mono text-sm">{organization?.id}</dd></div></dl>{role==="owner"&&<p className="mt-6 rounded-lg bg-amber-50 p-3 text-sm text-amber-800">Owner-only organization transfer and deletion remain intentionally unavailable until a verified recovery workflow exists.</p>}</Card></div>}
