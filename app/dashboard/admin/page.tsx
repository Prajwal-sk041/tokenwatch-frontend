"use client";import{useEffect,useState}from"react";import{getAdminOverview}from"@/lib/api";import{Card,PageHeader}from"@/components/page-header";
type O={users:number;organizations:number;active_subscriptions:number;trials:number;mrr:number;arr:number;monthly_requests:number;monthly_tokens:number};
export default function Page(){const[o,setO]=useState<O|null>(null);const[denied,setDenied]=useState(false);useEffect(()=>{getAdminOverview().then(r=>setO(r.data)).catch(()=>setDenied(true))},[]);if(denied)return <div><PageHeader title="Administration" description="Platform administrator access is required."/></div>;return <div className="space-y-6"><PageHeader title="Administration" description="Internal operational and revenue overview. Access is enforced by the backend."/><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Users",o?.users],["Organizations",o?.organizations],["Active subscriptions",o?.active_subscriptions],["Trials",o?.trials],["MRR",`$${Number(o?.mrr||0).toFixed(2)}`],["ARR",`$${Number(o?.arr||0).toFixed(2)}`],["30-day requests",o?.monthly_requests],["30-day tokens",o?.monthly_tokens]].map(([label,value])=><Card key={String(label)}><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-2xl font-semibold">{Number.isFinite(value as number)?Number(value).toLocaleString():value||"—"}</p></Card>)}</div></div>}
