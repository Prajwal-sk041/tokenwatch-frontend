"use client";import{FormEvent,useCallback,useEffect,useState}from"react";import API,{deleteAlert,getAlertHistory,getAlerts}from"@/lib/api";import{useWorkspace}from"@/components/app-shell";import{Card,PageHeader}from"@/components/page-header";
type A={id:string;name:string;threshold:number;period:string;channel:string;is_active:boolean};type H={id:string;status:string;channel:string;triggered_at:string};
export default function Page(){const{role}=useWorkspace();const[rows,setRows]=useState<A[]>([]);const[history,setHistory]=useState<H[]>([]);const load=useCallback(()=>Promise.all([getAlerts(),getAlertHistory()]).then(([a,h])=>{setRows(a.data);setHistory(h.data)}),[]);useEffect(()=>{void load()},[load]);const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);await API.post("/alerts/create",{alert_type:f.get("metric"),threshold:Number(f.get("threshold")),provider:f.get("provider"),period:f.get("period"),channel:f.get("channel"),destination:f.get("destination")||null});e.currentTarget.reset();await load()};return <div className="space-y-6"><PageHeader title="Alerts" description="Email and HTTPS webhook delivery are supported. Slack and Teams remain coming-soon stubs and cannot be selected."/>{role!=="viewer"&&<Card><form onSubmit={submit} className="grid gap-3 md:grid-cols-3"><select name="metric" aria-label="Metric" className="rounded border p-2"><option>cost</option><option>tokens</option><option>requests</option></select><input required type="number" min="0.01" step="0.01" name="threshold" aria-label="Threshold" placeholder="Threshold" className="rounded border p-2"/><select name="provider" aria-label="Provider" className="rounded border p-2"><option>all</option><option>openai</option><option>anthropic</option><option>gemini</option></select><select name="period" aria-label="Period" className="rounded border p-2"><option>daily</option><option>monthly</option></select><select name="channel" aria-label="Channel" className="rounded border p-2"><option>email</option><option>webhook</option><option disabled>slack — coming soon</option><option disabled>teams — coming soon</option></select><input name="destination" aria-label="Destination" placeholder="Email or HTTPS webhook" className="rounded border p-2"/><button className="rounded bg-slate-900 p-2 text-white md:col-span-3">Create alert</button></form></Card>}<div className="grid gap-5 lg:grid-cols-2"><Card><h2 className="font-semibold">Rules</h2><div className="mt-4 space-y-3">{rows.length===0?<p className="text-slate-500">No alert rules configured.</p>:rows.map(a=><div key={a.id} className="rounded border p-3"><div className="flex justify-between"><p className="font-medium">{a.name}</p><span className="text-xs">{a.is_active?"Active":"Paused"}</span></div><p className="text-sm text-slate-500">{a.channel} · {a.threshold} · {a.period}</p>{role!=="viewer"&&<div className="mt-2 flex gap-3"><button className="text-sm" onClick={async()=>{await API.patch(`/alerts/toggle/${a.id}`);await load()}}>{a.is_active?"Pause":"Enable"}</button><button className="text-sm text-rose-700" onClick={async()=>{if(confirm("Delete alert?")){await deleteAlert(a.id);await load()}}}>Delete</button></div>}</div>)}</div></Card><Card><h2 className="font-semibold">Delivery history</h2><div className="mt-4 space-y-3">{history.length===0?<p className="text-slate-500">No alert deliveries yet.</p>:history.map(h=><div key={h.id} className="flex justify-between border-b pb-2 text-sm"><span>{h.channel} · {h.status}</span><span>{new Date(h.triggered_at).toLocaleString()}</span></div>)}</div></Card></div></div>}
