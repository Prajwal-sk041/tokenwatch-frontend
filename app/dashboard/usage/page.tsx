"use client";
import { useCallback,useEffect,useState } from "react";import{getUsageEvents}from"@/lib/api";import{Card,PageHeader}from"@/components/page-header";
type E={id:string;provider:string;model:string;total_tokens:number;calculated_cost:number;project:string;environment:string;created_at:string};
export default function Page(){const[items,setItems]=useState<E[]>([]);const[page,setPage]=useState(1);const[more,setMore]=useState(false);const[provider,setProvider]=useState("");const load=useCallback(()=>getUsageEvents({page,page_size:25,...(provider?{provider}:{})}).then(r=>{setItems(r.data.items);setMore(r.data.has_more)}),[page,provider]);useEffect(()=>{void load()},[load]);const csv=()=>{const body=["timestamp,provider,model,tokens,cost,project,environment",...items.map(x=>[x.created_at,x.provider,x.model,x.total_tokens,x.calculated_cost,x.project,x.environment].join(","))].join("\n");const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([body],{type:"text/csv"}));a.download="tokenwatch-usage.csv";a.click()};return <div className="space-y-6"><PageHeader title="Usage" description="Paginated real usage events. Times are displayed in your browser timezone; stored timestamps and aggregates use UTC."/><Card><div className="flex flex-wrap gap-3"><select aria-label="Provider filter" value={provider} onChange={e=>{setProvider(e.target.value);setPage(1)}} className="rounded border p-2"><option value="">All providers</option><option>openai</option><option>anthropic</option><option>gemini</option><option>groq</option></select><button onClick={csv} disabled={!items.length} className="rounded border px-3 py-2 text-sm">Export current page CSV</button></div><div className="mt-5 overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b text-left text-slate-500"><th className="p-2">Time</th><th>Provider</th><th>Model</th><th>Project</th><th>Environment</th><th>Tokens</th><th>Cost</th></tr></thead><tbody>{items.map(x=><tr key={x.id} className="border-b"><td className="p-2">{new Date(x.created_at).toLocaleString()}</td><td>{x.provider}</td><td>{x.model}</td><td>{x.project}</td><td>{x.environment}</td><td>{x.total_tokens.toLocaleString()}</td><td>${Number(x.calculated_cost).toFixed(6)}</td></tr>)}</tbody></table>{items.length===0&&<p className="p-10 text-center text-slate-500">No usage matches these filters.</p>}</div><div className="mt-4 flex justify-between"><button disabled={page===1} onClick={()=>setPage(p=>p-1)} className="rounded border px-3 py-1 disabled:opacity-30">Previous</button><span>Page {page}</span><button disabled={!more} onClick={()=>setPage(p=>p+1)} className="rounded border px-3 py-1 disabled:opacity-30">Next</button></div></Card></div>}
